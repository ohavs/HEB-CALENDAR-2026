<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>

    <!-- מסך הבחירה של וידג׳ט הרשימה המשותפת. נפתח מעל מסך הבית ולא
         בתוך האפליקציה, ולכן הוא לא לוקח את ערכת הפתיחה עם ה-splash -
         רקע מסך הפתיחה היה מהבהב לרגע על שאלה של שתי שורות. -->
    <style name="AppTheme.WidgetConfig" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/widget_well</item>
    </style>
</resources>