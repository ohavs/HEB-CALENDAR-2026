# CLAUDE.md

הנחיות לעבודה על המאגר הזה. הקובץ נכתב לקראת ההמרה לאנדרואיד, ולכן הוא
מדגיש את מה שקל לשבור בלי לשים לב.

## פקודות

```bash
npm run dev         # שרת פיתוח
npm test            # כל הבדיקות (vitest run)
npm run test:watch
npm run lint        # eslint
npm run typecheck   # tsc -b --noEmit
npm run build       # typecheck + build לייצור
PREVIEW_BUILD=1 npm run build   # בלי service worker ובלי manifest

npm run icons:android   # משאבים גרפיים לאנדרואיד
npm run android:sync    # build + npx cap sync android
```

`npm run lint`, `npm test` ו-`npm run build` רצים בפריסה. מה שנשבר לא
מגיע לאוויר.

## מבנה

```
src/lib/        לוגיקה טהורה, בלי React. כאן נמצא כל מה שאפשר לבדוק.
src/store/      zustand עם persist. אירועים, הגדרות, חשבון.
src/hooks/      הוקים משותפים.
src/components/ ה-UI. components/ui/ הם הרכיבים הבסיסיים.
```

הפרדה שכדאי לשמור: **`src/lib` לא מייבא מ-`src/components`.** זה מה
שמאפשר לבדוק את הלוגיקה בלי דפדפן, ומה שיאפשר להעביר אותה כמות שהיא
לאנדרואיד.

## כללים שאסור לשבור

### תאריכים הם מקומיים, לעולם לא UTC

```ts
dateKey(d)   // ✓ YYYY-MM-DD בשעון המקומי
d.toISOString().slice(0, 10)   // ✗ יזוז ביום שלם בערב
```

כל מפתח תאריך באפליקציה עובר דרך `dateKey()` ב-`src/lib/dates.ts`. אירוע
שנוצר ב-23:30 חייב להישאר באותו יום. יש בדיקות שתופסות את זה.

### זמנים תלויים באזור זמן, לא בהיסט

הזמנים מחושבים לפי מזהה IANA (`Asia/Jerusalem`) ולא לפי היסט קבוע, אחרת
שעון קיץ וחורף יוצאים שגויים. הבדיקות נעולות ל-`Asia/Jerusalem`
ב-`vitest.config.ts`.

### אדר א׳ ואדר ב׳

חזרה שנתית לפי התאריך העברי חייבת להתמודד עם מעבר בין שנה פשוטה למעוברת:
אירוע באדר של שנה פשוטה חוזר באדר **ב׳** של שנה מעוברת, ולהפך. הלוגיקה
ב-`hebrewMonthMatches` ב-`src/lib/recurrence.ts`, ויש לה בדיקות.

### חריגים נשמרים לפי התאריך המקורי

מפת `exceptions` על אירוע ממופתחת לפי התאריך שבו המופע **היה אמור** לחול,
גם אחרי שהוזז. זה מה שמאפשר להזיז מופע שוב או להחזיר אותו למקומו. אם
תמפתח לפי התאריך החדש, כל הזזה תיצור חריג נוסף.

### מיון המועדים

`classify()` ב-`src/lib/hebrew.ts` בודק את דגל החג **לפני** דגל הצום.
יום כיפור נושא את שניהם, ואם הוא מסווג כצום - כיבוי "הצגת צומות"
בהגדרות מעלים אותו מהלוח. זה היה באג אמיתי.

## מוסכמות

- **הערות ותיעוד בעברית.** הקוד באנגלית, מה שמסביר אותו בעברית.
- **הערה מסבירה למה, לא מה.** אם ההערה מתארת את מה שהשורה עושה, היא
  מיותרת. אם היא מתארת החלטה, אילוץ או מלכודת - היא חיונית.
- **RTL בכל מקום.** ב-RTL הזמן זורם שמאלה: "הבא" בשמאל עם חץ שמאלה,
  "הקודם" בימין עם חץ ימינה. להשתמש ב-`start`/`end` ולא ב-`left`/`right`.
- **טוקנים, לא מספרים.** תנועה (`TAP`/`SNAP`/`GLIDE`), גדלי אייקונים
  (`ICON.*`), גובה (`shadow-raised`/`floating`/`overlay`) - הכול
  ב-`src/lib/motion.ts` וב-`tailwind.config.js`. אין קפיץ חדש ברכיב.
- **סולם המישוש הוא חלק מאותה מערכת.** `haptic()` ב-`native.ts`, בשתי
  דרגות, והכלל מתי לקרוא:

  | מה קרה | דרגה |
  |---|---|
  | בחירה, ניווט, פתיחת גיליון | שקט |
  | שינוי מצב: מתג, צ׳קבוקס, צ׳יפ, קיפול | `light` |
  | כתיבה או מחיקה, והרמה בגרירה | `medium` |

  באנדרואיד האצבע מצפה לתשובה, וכשהיא לא מגיעה המסך מרגיש מוקלט מראש.
  זה ההבדל הכי מורגש בין "אתר בתוך WebView" לאפליקציה.
- **בוררים משלנו.** אין `<input type="date">` ואין `<select>` באפליקציה:
  הם נראים אחרת בכל מכשיר. יש `DateField`, `TimeField`, `NumberField`,
  `SelectField` ב-`src/components/ui/`.
- **כל גיליון עובר דרך `Sheet`.** הוא מטפל בגרירה לסגירה, ב-Esc, ובכפתור
  החזרה של אנדרואיד. רכיב שמצייר גיליון בעצמו יאבד את כל השלושה.
- **שער היציאה יושב ב-`Sheet`, לא בטופס.** לגיליון ארבעה מוצאים - גרירה
  למטה, הקשה על הרקע, Esc וכפתור החזרה - וכולם שקטים: אצבע שהחליקה קצת
  יותר מדי מחקה טופס שמולא בלי שום סימן. `beforeClose` מחזיר `false`
  ואז הטופס שואל בעצמו (`ConfirmDiscardSheet`), והשאלה עולה רק כשיש מה
  לאבד - `isDraftDirty` מול הטופס כפי שנפתח. שער שהיה יושב על מוצא אחד
  רק היה מעביר את אובדן המידע למוצא הבא.

  מכאן נגזרת מלכודת בכפתור החזרה: ה"חזרה" כבר צרכה את הרשומה
  מההיסטוריה עד שהשער נשאל. לכן `onPopState` ב-`overlayHistory` דוחף
  אותה בחזרה כששכבה סירבה להיסגר - בלי זה הלחיצה הבאה הייתה יוצאת
  מהאפליקציה בזמן שהגיליון עוד פתוח. שמירה ומחיקה אינן עוברות בשער:
  הן קוראות ל-`onClose` ישירות, ואין להן מה לאבד.
- **גרירת מגע נעצרת רק ב-`touchmove`.** `preventDefault` על `pointermove`
  אינו מונע גלילת מגע. במסך נגלל הדפדפן מחליט בעצמו שהתנועה היא גלילה,
  שולח `pointercancel` ומפסיק לשלוח `pointermove` - ובלוח זה לא הופיע
  רק מפני שהרשת אינה נגללת. לכן `dragEngine` מאזין גם ל-`touchmove` עם
  `passive: false`, וגרירה שכבר התחילה מתעלמת מ-`pointercancel`. אותו
  שורש בדיוק כמו ב-`useSheetDrag`.
- **יעד המחיקה נמדד, לא נדגם.** העיגול שצף בתחתית המסך בזמן גרירה הוא
  `pointer-events-none` - אחרת הוא היה בולע את הגרירה עצמה ומסתיר את
  הימים שמתחתיו - ולכן `elementFromPoint` לעולם לא יחזיר אותו. במקום
  זאת `dragEngine` מודד את המלבן שלו ישירות (`[data-drag-trash]`),
  והמחיקה גוברת על היום שמתחת. האישור עצמו שייך למסך ולא למנוע: מופע
  בסדרה חוזרת נשאל "מה למחוק", וכל השאר נשאל פעם אחת.
- **מה שהמשתמש הכניס קודם למועדים בתא הלוח.** קודם היה מובטח מועד אחד
  תמיד, והאירועים קיבלו את מה שנשאר - ובחודש עמוס במועדים כמו תשרי זה
  דחק החוצה בדיוק את מה שהמשתמש בא לראות. מועד חוזר כל שנה ויש לו
  לשונית משלו; פגישה ב-15:00 קיימת רק כאן. מה שנדחק נספר במונה בפינה.
- **מה שהשדה מציג חייב להיות מה שהטיוטה מחזיקה.** נפילה לאחור שנכתבת
  ב-JSX (`value={draft.endTime ?? '10:00'}`) יוצרת ערך שהמשתמש רואה
  ואף חישוב אינו מכיר: בתזכורת, ששמורה בלי שעות, כיבוי "כל היום" חשף
  09:00 ו-10:00 מדומים - ולכן הזזת ההתחלה לא הזיזה את הסיום, שנשאר על
  אותה עשר. הנפילה לאחור שייכת לאתחול הטיוטה, לא לתצוגה.
- **אין `layoutId` בתוך גיליון.** אנימציית פריסה משותפת נשארת תלויה
  כשהעץ שמכיל אותה מתפרק, ו-`AnimatePresence` של `Sheet` ממתין לה
  לנצח: הגיליון נשאר בדום אחרי שהוא כבר נעלם, וחוסם כל לחיצה. זה קרה
  ל-`Segmented`, שעבר למדידה של `offsetLeft`.

## מיקומים

שני דברים נפרדים שקל לבלבל ביניהם:

- **מקום של אירוע** (`location` + `placeId`) הוא כיתוב. ההצעות מגיעות
  מ-`suggestLocations()` ב-`src/lib/eventLocations.ts`.
- **מקום שמור** (`SavedPlace`) הוא נקודת ציון ורדיוס, ומשמש לגדר
  הגאוגרפית.

נקודת ציון נקבעת בלי רשת: `parseCoordinates()` ב-`src/lib/coordinates.ts`
מפענח הדבקה ממפות גוגל, אפל, ווייז, `geo:`, מעלות־דקות־שניות וזוג עשרוני.
קישור מקוצר אינו נתמך בכוונה - `isShortLink()` מזהה אותו כדי שאפשר יהיה
להסביר למשתמש במקום להיכשל בשקט.

אין גיאוקודינג. הוספה שלו תשלח כל תו שהמשתמש מקליד לצד שלישי, וזו החלטה
שצריך לקבל במפורש; הטבלה ב-README מפרטת את האפשרויות.

## נגישות

הלוח הוא `role="grid"` עם roving tabindex. לגרירת אירוע יש מקבילה
במקלדת (Alt+חיצים). פעולה שמשנה את המסך בלי להזיז את המיקוד מדווחת דרך
`announce()` ב-`src/lib/announce.ts`: מעבר חודש, החלפת לשונית, שמירה
ומחיקה, סימון תזכורת, סינון סדר יום, והזזת אירוע בחיצים.

הכלל הזה קל לשכוח כי שום דבר לא נשבר בלעדיו. הבדיקה הפשוטה: אם אחרי
הלחיצה המסך אחר והמיקוד נשאר במקום, צריך הכרזה.

## אנדרואיד

האפליקציה נארזת ל-APK דרך Capacitor, וה-WebView טוען את אותו `dist` שהאתר
טוען. אין גרסה שנייה של הקוד.

**כל מה שנוגע למערכת ההפעלה עובר דרך `src/lib/native.ts`, ורק דרכו.**
אין בקובץ הזה ייבוא סטטי של אף פלאגין: `isNative()` בודק את הגשר
שקפסיטור מזריק ל-window, וכל פלאגין נטען ב-import דינמי רק אחרי תשובה
חיובית. זה מה ששומר על `src/lib` טהור ועל הבדיקות רצות ב-node. פלאגין
שייובא סטטית באחד מקבצי הלוגיקה ישבור את שניהם.

| מה | בדפדפן | באנדרואיד |
|---|---|---|
| תזכורות | טיימרים בדף + service worker | `scheduleNativeReminders()` - התראות מקומיות |
| מיקום | `navigator.geolocation` | `@capacitor/geolocation` |
| התחברות | `signInWithPopup` | Credential Manager עם idToken |
| כפתור החזרה | היסטוריית הדפדפן | אותה היסטוריה (`overlayHistory.ts`) |

**פלאגין לעולם לא מוחזר ישירות מפונקציה אסינכרונית.** הוא Proxy שכל
מאפיין בו הוא קריאה נייטיבית - גם `then`. `await` על ערך כזה קורא ל-`then`
כאילו היה הבטחה, והקריאה נדחית בלי לענות: ה-`await` תקוע לנצח, בלי שגיאה.
כך הגדרות המקום לא נרשמו מעולם, ומתג היומן לא הופיע. מחזירים
`{ plugin }`, ו-`pluginProxy.test.ts` סורק את הקוד.

`buildReminders()`, `evaluatePosition()` ו-`buildDay()` לא ידעו על השינוי
הזה בכלל - הם היו טהורים מלכתחילה, וזה מה שאיפשר את ההחלפה הנקודתית.

### תזכורות

**תזכורת היא אירוע.** אותה ישות, אותו מנוע, אותו סנכרון. ההבדל היחיד
הוא הדגל `undated` - פריט שאינו משויך ליום: `expandEvents` מדלג עליו,
ולכן הוא לא קיים בלוח כלל, והוא נאסף לקבוצה אחת בראש מסך התזכורות.

למה דגל ולא `date: null`: מפתח התאריך הוא האינדקס של כל המנוע - הרחבת
חזרות, חריגים, גדר גאוגרפית ותזכורות - ולהפוך אותו לאופציונלי היה מפזר
בדיקת null על פני כל הקוד, בשביל מצב אחד.

המודל ב-`src/lib/reminders.ts`, והוא משרת גם את המסך וגם את הוידג׳ט.
שני חישובים נפרדים היו נפרדים גם בתוצאה.

**תזכורת עם יום מקבלת שעה, לא "כל היום".** `src/lib/reminderCompose.ts`
גוזר את `allDay` מהשעה ולא קובע אותו בנפרד, ולכן ההוספה המהירה והעורך
אומרים את אותו דבר. יום שנבחר מקבל שעה מוצעת מיד - לחצי השעה העגולה
הבאה כשזה היום, ובוקר כשזה יום אחר - ומי שאינו רוצה שעה מנקה אותה
במפורש. "בלי תאריך" מאפס אותה: שעה בלי יום אינה שעה.

ההוספה המהירה נשארת שורה אחת, ומה שדורש מקום, חזרה או הערות ממשיך
לעורך המלא דרך `prefill` - בלי לשמור בדרך, כדי שביטול לא ישאיר אחריו
תזכורת שאיש לא ביקש.

**שורת האפשרויות היא רכיב אחד לשתי הרשימות.** `ComposeRow` ב-
`src/components/ui/` משרת את התזכורות האישיות ואת הרשימה המשותפת כאחד,
ושתיהן בונות את הפריט דרך `buildReminderDraft`. פריט משותף הוא
`UserEvent` בתוספת שיוך, ולכן השאלה "יש יום? יש שעה?" נענית בשניהם
אותו דבר - ושתי שורות נפרדות היו נפרדות גם בהתנהגות ברגע שאחת תשתנה.
ב"עוד" של הרשימה המשותפת הפריט נמסר ל-`SharedItemSheet` כ-`draft`:
הוא טרם נכתב לענן, ולכן "להסתיר מהלוח שלי" אינה מוצגת שם - היא כותבת
לנתיב של מסמך שאינו קיים.

**מה שסומן "בוצע" יורד לתחתית הקבוצה.** `sinkDone` ב-`reminders.ts`
עושה חלוקה יציבה ולא מיון, כדי לא לערבב את הסדר שכבר נקבע בין
הפתוחות. זה נמצא במודל ולכן חל גם על הוידג׳ט - אבל **גם הצד הנייטיבי
מסדר מחדש בעצמו**, ב-`WidgetActionReceiver`, כי הוידג׳ט מצייר את
תמונת המצב שלו מיד; בלי זה הפריט היה נשאר במקומו עד העלייה הבאה של
האפליקציה, בזמן שבמסך הוא כבר ירד.

**מה שבוצע נמחק מעצמו אחרי יום.** `sweepDone` ב-`reminders.ts` מחזיר את
המזהים, ו-`App` מריץ אותו בעלייה ובכל חזרה לחזית - לא בטיימר, כי
האפליקציה בטלפון אינה פתוחה שעות. `doneAt` על החריג הוא מה שמאפשר את
זה: הדגל לבדו בוליאני, ו-`updatedAt` של האירוע זז בכל עריכה.

שני גבולות שהמחיקה הזו מצומצמת בהם בכוונה, כי אין ממנה דרך חזרה במסך:
**אירוע חוזר אינו נמחק** - מחיקת סדרה בגלל מופע אחד שבוצע הייתה מוחקת
גם את מה שעוד לא קרה - ו**החלון הוא הגדרה** (`doneCleanupDays`, ואפס
מכבה). המחיקה עצמה היא `remove` הרגיל, כלומר סימון שמסתנכרן.

הגרירה במסך התזכורות עוברת דרך מנוע הגרירה של הלוח: כל קבוצה נושאת
`data-day-key`, בדיוק כמו תא. `undated` הוא "יום" לכל דבר מבחינת המנוע,
וזה מה שמאפשר לגרור פריט אל מחוץ ללוח ובחזרה.

### "הוסף ליומן" וקובצי הזמנה

באנדרואיד אין "לוח שנה ברירת מחדל" אחד - כל פעולה נבחרת בנפרד, עם
"תמיד". שתיים מוכרזות ב-manifest: `ACTION_INSERT` של אירוע (מה שמייל או
דפדפן שולחים ב"הוסף ליומן"), ו-`VIEW` של `text/calendar`. סנכרון דו־כיווני
מול ספק היומן של המערכת נשקל ונדחה: זה מנוע סנכרון שני.

`ExternalIntents.java` מתרגם כל כוונה כזו לקישור `hebcal://open?ext=...`,
כך שהיא עוברת באותו מסלול של הוידג׳טים ואין בדף ערוץ שני. שתי מלכודות:

- **התרגום קודם ל-`super.onCreate`**, כי הגשר קורא את הכוונה כבר בבנייה.
  בשחזור (`savedInstanceState`) הכוונה עדיין המקורית, והיא נזרקת - אחרת
  העורך היה נפתח שוב.
- **הקובץ נקרא ב-Activity, לא בדף.** ההרשאה ל-`content://` ניתנה לה בלבד
  ולזמן קצר. העותק נשמר במטמון ונלקח פעם אחת (`takeIncomingCalendar`).

השדות הופכים לטיוטה ב-`externalEvent.ts`, והעורך נפתח מולא - **לא
נשמר.** המידע הגיע ממישהו אחר, והמשתמש מאשר. "כל היום" מגיע בחצות UTC
והסיום שלו בלעדי; שניהם מטופלים שם, עם בדיקות. קובץ עם אירוע אחד נפתח
בעורך, וקובץ עם כמה הוא ייבוא דרך `store/importEvents.ts` - אותו כלל
כפילויות של הגיבוי, וביטול אחד לכולם.

**בחירה ב"הוסף ליומן" אינה רושמת אותנו ביומן של המכשיר.** וידג׳טים
ולוחות של אפליקציות אחרות קוראים רק את `CalendarContract`, ולכן יש פרסום
נפרד: `HebCalendarPlugin` יוצר לוח מקומי בשם "לוח עברי" וממלא אותו
מ-`buildSystemEvents` ב-`src/lib/systemCalendar.ts`. כיוון אחד בלבד, והלוח
לקריאה בלבד (`CAL_ACCESS_READ`) - עריכה בלוח אחר הייתה נראית שמורה ונמחקת
בפרסום הבא. זה מתג בהגדרות (`systemCalendar`), כי הוא דורש הרשאת יומן.

- **כל מופע הוא אירוע שטוח, בלי RRULE.** `expandEvents` יודע אדר ב׳,
  חריגים ומופעים שהוזזו, ו-RRULE לא. המחיר הוא חלון - חודש אחורה, שנה
  קדימה - שזז בכל עלייה.
- **אירוע רב־יומי נכתב פעם אחת.** `expandEvents` מחזיר שורה לכל יום
  בפרישה, ולכן רק `spanIndex === 0` נכתב.
- **הכתיבה היא בשם מתאם סנכרון** (`CALLER_IS_SYNCADAPTER`). רק כך מותר
  ליצור לוח מקומי, ורק כך מחיקה מוחקת ולא מסמנת `DELETED` לנצח.
- **בלי התראות משלו** (`HAS_ALARM` 0) - האפליקציה כבר מתריעה.
- כיבוי **מוחק** את הלוח: לוח קפוא היה מציג אירועים שכבר זזו.

### תבניות אירוע

**תבנית היא אירוע בלי תאריך** - אותו רעיון של "תזכורת היא אירוע", ומאותה
סיבה: כל מה שאינו תאריך זהה בשניהם. `src/lib/templates.ts` הוא המודל,
והוא טהור.

מה שתבנית אינה: היא לא יושבת על הלוח, לא מתפשטת ולא חוזרת. `expandEvents`
אינו יודע עליה דבר.

**האירוע שנוצר מנותק מהתבנית.** אין קשר חי, וזו החלטה: קשר כזה היה אומר
שמחיקת "חופש מהעבודה" מוחקת חופשות שכבר נלקחו. `templateToEvent()` הוא
העתקה חד־כיוונית, ו-`repeat` בו הוא תמיד `none`.

היא יושבת בהגדרות (`settings.templates`) ולא בחנות משלה, מאותו שיקול של
מקומות שמורים: פריט קטן שמתואר פעם אחת, משמש שוב ושוב, וצריך להסתנכרן.
יש תקרה (`MAX_TEMPLATES`), כי ההגדרות נוסעות במסמך אחד שיש לו חסם בכללים.

שתי דרכים לשבץ, ושתיהן נפגשות ב-`placeTemplateOn` ב-`App.tsx` - מקור אחד,
כדי שלא יתפצלו:

- **לשונית "תבניות" בתזכורות** - שם בונים אותן, ושם משבצים לכמה ימים
  בבת אחת דרך `TemplatePlaceSheet`. זו הדרך העיקרית, כי תבנית נועדה
  לכמה ימים ולא לאחד.
- **רצועה בראש חלונית היום** - הקשה משבצת ליום הפתוח, גרירה מפילה על
  יום אחר. הגרירה עוברת דרך מופע סינתטי עם הקידומת `tpl:`, ו-`onDrop`
  מזהה אותה ויוצר במקום להזיז - כך מנוע הגרירה אינו צריך לדעת שתבניות
  קיימות בכלל.

שיבוץ לכמה ימים הוא פעולה אחת מבחינת המשתמש, ולכן יש לו הודעת ביטול
אחת שמסירה את כולם - ולא אחת לכל יום.

### רשימות משותפות

לשונית שנייה בתוך מסך התזכורות, ולא ערבוב ברשימה האישית: השאלה "מי עוד
רואה את זה" חייבת להיות ברורה מהמסך.

ההבדל המהותי אינו בתוכן אלא בבעלות, וממנו נגזר הכול. התזכורות האישיות הן
local-first - הן חיות במכשיר, נדחפות לענן כגיבוי, וממוזגות
last-write-wins מול מכשירים אחרים של *אותו* משתמש. רשימה משותפת היא
ההפך: **היא חיה בענן**, ושני אנשים יכולים לכתוב אליה באותה שנייה. לכן
היא אינה עוברת ב-`persist` של zustand ואינה ממוזגת מקומית - נקראת
ב-`onSnapshot`, וכתיבה נשלחת ישירות. מטמון ה-SDK נותן קריאה בלי רשת.

- `src/lib/sharedLists.ts` - המודל הטהור. טיפוסים, מפתחות, סינון וספירה.
- `src/lib/sharedSync.ts` - כל מה שנוגע לרשת. שלושה מאזינים: הרשימות
  שאני חבר בהן, ההזמנות שממתינות לי, והפריטים של הרשימה הפתוחה בלבד.
- `src/store/shared.ts` - החנות. בלי `persist`; רק העדפות התצוגה נשמרות.

**מזהה ההזמנה נגזר, ולא אקראי.** `inviteId()` מרכיב `<listId>__<email>`,
כי כלל אבטחה ב-Firestore אינו יכול *לחפש* מסמך - רק לבדוק קיום של נתיב
ידוע. זה מה שמאפשר לכלל לוודא שמי שמצטרף באמת הוזמן, בלי Cloud Functions.
האימייל מנורמל בשני הצדדים (`.lower()` בכלל, `normalizeEmail()` בקוד), ואם
הנרמול ייפרד - ההצטרפות תיכשל בלי הודעה מובנת. יש לזה בדיקות.

הפריט הוא `UserEvent` בתוספת `categoryId` ו-`createdBy`, ולכן `expandEvents`
ו-`buildReminderGroups` עובדים עליו כמו שהם.

**פריט משותף עם תאריך יושב גם בלוח.** `SharedItem` הוא `UserEvent`,
ולכן `expandEvents` מרחיב אותו כמו כל אירוע - חזרות, פרישה רב־יומית
וחריגים. `src/lib/sharedCalendar.ts` הוא המיזוג: הוא מסנן למה ששייך
ללוח *שלי* ומסמן כל מופע ב-`shared` עם שם הרשימה.

מכאן נגזרת מלכודת: **אותו כרטיס נושא שתי ישויות שונות.** אירוע אישי חי
בחנות המקומית, פריט משותף חי בענן, והם נראים זהים. קריאה לחנות המקומית
על פריט משותף אינה נכשלת - היא פשוט *לא עושה כלום*, כי המזהה אינו
ב-`byId`, ומבחינת המשתמש ההקשה נבלעה. לכן כל פעולה על מופע עוברת דרך
`useOccurrenceActions`, ו-`App` מנתב עריכה, גרירה ומחיקה לפי הדגל.

**התאריך משותף, הנוכחות בלוח אישית.** `hiddenBy` היא מפה של uid על
הפריט עצמו, והכתיבה אליה היא לנתיב מנוקד (`hiddenBy.<uid>`) - כך ששני
חברים שמסתירים באותה שנייה אינם דורסים זה את זה ולא את התאריך.

`npm run test:rules` מריץ את כללי האבטחה מול האמולטור - כולל כל ניסיון
הסלמה. הוא דורש את אמולטור Firestore ואינו חלק מ-`npm test`.

**התראה על הזמנה מגיעה מהשרת, לא מהמכשיר.** Firestore אינו דוחף דבר:
מסמך הזמנה חדש מגיע לנמען רק אם הוא מחובר ומאזין באותו רגע. לכן יש
פונקציה אחת במאגר - `functions/index.js` - שמאזינה ליצירת הזמנה ושולחת
FCM. שתי הפעולות שלה אפשריות רק לצד מהימן: תרגום אימייל ל-uid
(`getUserByEmail` הוא admin בלבד), וקריאת הטוקנים של הנמען מתוך
`users/{uid}/push`, שאיש מלבדו אינו קורא.

היא דורשת את תוכנית Blaze. בלעדיה הפריסה נכשלת, וזה שלב נפרד ב-workflow
בכוונה - כדי שכשל שלה לא ימנע מהאתר ומהכללים לעלות.

### התחברות בכניסה קרה

**Auth של אנדרואיד נוצר בלי `popupRedirectResolver`.** `getAuth` של web
מצרף אותו, ובעלייה הוא טוען iframe מהדומיין של Firebase כדי לבדוק אם
חזרנו מהפניה - ו-`onAuthStateChanged` אינו יורה עד שהבדיקה נגמרת. זה מה
שהפך כל כניסה קרה, למשל מהוידג׳ט, לשניות של "עוד לא מחובר". באנדרואיד אין
הפניות בכלל, ולכן `createAuth` ב-`firebase.ts` משתמש ב-`initializeAuth`,
ו-`getRedirectResult` נקרא רק בדפדפן.

ההתמדה נשארת `indexedDBLocalPersistence` בכוונה: `getAuth` מציב אותה
ראשונה ברשימה, ולכן שם החיבור הקיים יושב. החלפה שלה הייתה מנתקת את כל מי
שכבר מחובר.

**`profile` הוא לתצוגה, `user` הוא האמת.** הזהות האחרונה שנראתה נשמרת,
כדי שהתמונה והשם יופיעו מיד ולא אחרי שניות. אבל היא לא אומתה, ולכן **שום
דבר שמדבר עם הענן לא קורא אותה** - סנכרון שמתחיל בלי אסימון נכשל בהרשאות.
`authProfile.test.ts` סורק את הקוד ומוודא שרק מקומות התצוגה המוכרים קוראים
אותה. Firebase מקבל את ההחלטה הסופית: כשהוא עונה "אין משתמש", גם
`profile` יורד.

Firebase מתחיל להיטען ב-`main.tsx`, במקביל לציור הראשון, למי שהיה מחובר.
קודם הטעינה חיכתה ל-`useEffect` ב-`App` - כלומר לסוף ציור הלוח כולו.

**תמונת הפרופיל נטענת עם `referrerPolicy="no-referrer"`.** שרתי התמונות
של גוגל דוחים Referer ממקור שאינם מכירים, וה-WebView שולח `localhost` -
כשל שמופיע ונעלם ולכן נראה אקראי. ל-`Avatar` יש ניסיון חוזר אחד (הגבלת
קצב חולפת), ניסיון נוסף בחזרה לרשת, וכישלון נגמר באות הראשונה ולא בעיגול
ריק. שמירת עותק מקומי של התמונה נשקלה ונדחתה: היא דורשת CORS מהשרת, ובלעדיו
תמונה עם `crossOrigin` נכשלת *תמיד* - הפוך ממה שרצינו.

### אחסון

**הענן הוא הבית, לא הגיבוי.** האירועים, התזכורות וההגדרות של כל משתמש
חיים בחשבון הגוגל שלו ב-Firestore, והאחסון המקומי הוא מטמון שמאפשר
עבודה בלי רשת.

זה שינוי בתפיסה ולא בקוד הסנכרון, שנשאר כפי שהיה - `startSync()` מושך,
ממזג ודוחף, וגם מעלה בפעם הראשונה את מה שנוצר לפני ההתחברות. מה שהשתנה
הוא שהשאלה נשאלת מראש: `WelcomeSheet` מוצג פעם אחת למי שלא התחבר, כי
אחסון ה-WebView נחשב מטמון מבחינת אנדרואיד - "ניקוי מטמון" או לחץ אחסון
מוחקים אותו בלי אזהרה.

אפשר להמשיך בלי חשבון, וזו החלטה מודעת של המשתמש ולא ברירת מחדל שקרתה
לו.

**`undefined` מפיל כתיבה שלמה ל-Firestore.** לא "מתעלם ממנו" - דוחה את
המסמך כולו: `Unsupported field value: undefined`. וזה קל במיוחד ליפול
לזה כי `JSON.stringify` דווקא מוריד את השדות האלה בשקט, ולכן האחסון
המקומי נראה תקין והכשל מופיע רק מול הענן. שדה רשות ריק נמחק
(`delete`), לא נשמר כ-`undefined`, ו-`stripUndefined()`
ב-`src/lib/firestoreSafe.ts` הוא רשת הביטחון על מסמך ההגדרות - שם
נוסעות התבניות והמקומות השמורים.

**כלל האבטחה הוא whitelist.** `hasOnly` ב-`validEvent` מונה את שדות
האירוע, ושדה שאינו ברשימה נדחה. שדה חדש חייב להתווסף בשלושה מקומות
יחד: `toDoc`, `toUserEvent` והכלל. `undated` היה חסר בכל השלושה, ולכן
תזכורת בלי תאריך איבדה את הדגל ברגע שעברה דרך הענן - וצצה בלוח ביום
שנשמר לה רק כדי שתדע לאן לחזור.

### התראות מבוססות מקום

**מערכת ההפעלה היא שמנטרת, לא אנחנו.** זה היה הבאג: המימוש הראשון היה
`watchPosition` בתוך ה-WebView, והוא חי רק כל עוד האפליקציה בחזית -
כלומר בדיוק לא ברגע שבו המשתמש בדרך לאנשהו. עכשיו `HebGeofencePlugin`
רושם גדרות אמיתיות ב-`GeofencingClient`, ו-`GeofenceReceiver` מופעל גם
כשהאפליקציה מתה לגמרי. המעקב בדף נשאר לדפדפן בלבד, שאין לו תחליף.

מכאן ארבעה דברים שקל לשבור:

- **`ACCESS_BACKGROUND_LOCATION` היא כל ההבדל.** בלעדיה הכול נראה תקין -
  המקום נשמר, המתג דלוק - וההתראה פשוט לא מגיעה. מ-API 30 אי אפשר לבקש
  אותה בדיאלוג, רק המשתמש בוחר "לאפשר תמיד" במסך ההגדרות; לכן מסך
  ההגדרות בודק ואומר את זה במפורש, במקום להיכשל בשקט.
- **המקלט אינו מריץ שום קוד שלנו**, בדיוק כמו וידג׳ט: אין React, אין
  hebcal, ואין דרך לגזור כותרת ממזהה. הכותרת, הגוף והיום נוסעים ערוכים
  ב-`buildFences` ונשמרים ב-`GeofenceStore`. מה שלא נשלח, לא יוצג.
- **הגדרות נרשמות לשבוע קדימה**, כדי שמי שלא פתח את האפליקציה בבוקר
  עדיין יקבל התראה. לכן `date` נוסע איתן, והמקלט משווה מחרוזות לפני
  שהוא מצלצל.
- **אנדרואיד מוחק את כל הגדרות באתחול המכשיר**, ולכן יש `BootReceiver`.
  ושתי מלכודות שנכשלות בזמן ריצה בלבד: `setLoiteringDelay` תקף רק עם
  מעבר DWELL ו-`build()` זורק בלעדיו, וההסרה וההוספה אסינכרוניות - בלי
  שרשור ההסרה עלולה למחוק את מה שזה עתה נרשם.

### וידג׳טים

ארבעה וידג׳טים: לוח, תזכורות, זמני שבת, ורשימה משותפת. כולם מצוירים על
ידי מערכת ההפעלה בתהליך אחר, גם כשהאפליקציה סגורה - ולכן **וידג׳ט לא
מריץ שום קוד שלנו.** הוא לא יכול לקרוא ל-hebcal, לא להרחיב אירוע חוזר ולא לעצב
תאריך עברי.

מכאן כל המבנה: `src/lib/widgetData.ts` בונה JSON שטוח ומוכן להצגה,
`useWidgets` מפרסם אותו בכל שינוי, והצד הנייטיבי רק מצייר. כל מחרוזת
שמוצגת בוידג׳ט חייבת להגיע ערוכה משם - כולל "היום" וכולל שעות.

**שדה חדש בנתוני הוידג׳ט צריך נפילה לאחור.** חבילת ה-web והמעטפת
הנייטיבית מתעדכנות בנפרד - החבילה בעדכון חי, המעטפת רק בהתקנת APK -
ולכן כל צירוף אפשרי: מעטפת חדשה מול נתונים ישנים, ונתונים חדשים מול
מעטפת ישנה. הצד הנייטיבי קורא את השדה החדש ונופל לישן כשהוא ריק
(`subLine` ב-`ShabbatWidgetProvider`), והצד שלנו ממשיך לשלוח את הישן.
בלי זה מי שלא התקין רואה שורה ריקה.

הכיוון ההפוך עובר בתור: סימון "בוצע" או הוספת תזכורת מהוידג׳ט מתבצעים
מיד על תמונת המצב שלו, ונרשמים לתור שהאפליקציה מיישמת בעלייה הבאה.
בלי העדכון המקומי לחיצה לא הייתה עושה כלום עד שהאפליקציה נפתחת.

שתי מגבלות של אנדרואיד שקבעו את העיצוב, ואין דרך לעקוף אותן:

- **אין שדה טקסט בוידג׳ט.** RemoteViews לא תומך ב-EditText, ולכן ההוספה
  היא `AddReminderActivity` - חלונית שנפתחת מעל מסך הבית.
- **רשימה נגללת חייבת RemoteViewsService**, וכל הלחיצות בתוכה עוברות
  דרך תבנית אחת עם כוונת מילוי.
- **וידג׳ט שיש בו מה לבחור צריך `android:configure`.** לוידג׳ט הרשימה
  המשותפת יש שתי בחירות - רשימה וקטגוריה - והן נשמרות לפי מזהה המופע
  (`WidgetStore.putSharedConfig`), כך שאפשר להניח שניים על אותה רשימה
  ולכוון כל אחד אחרת. מסך הבחירה הוא Activity רגילה ולא RemoteViews,
  ולכן אינו כפוף למגבלות שלמטה - ולכן גם הפריסות שלו אינן נקראות
  `widget_*`: הקידומת הזו שמורה למה שהבדיקה סורקת.
- **הסינון בצד הנייטיבי הוא השוואת מחרוזות בלבד.** פריט נושא `cat`, והשם
  להצגה של הקטגוריה מגיע מוכן מ-`widgetData.ts`. הוידג׳ט לא גוזר שם
  ממזהה.
- **`reconfigurable` הוא מה שמחזיר להגדרות בלחיצה ארוכה.** `configure`
  לבדו פותח את מסך הבחירה רק בהנחה הראשונה, וכדי לשנות היה צריך להסיר
  את הוידג׳ט ולהניח מחדש. שניהם יושבים ב-`widget_*_info.xml`, לצד
  `configuration_optional` - שמשאיר את ההנחה מיידית כשיש ברירת מחדל
  שעובדת. שלושתם מ-API 31; במכשיר ישן יותר המשגר מתעלם מהם, ולכן מסך
  הבחירה חייב להיות בטוח גם כשהוא נפתח בהנחה בלבד.
- **וידג׳ט התזכורות מכוון למקור אחד.** `sources` ב-`widgetData.ts` הוא
  מערך של תמונות מצב מקובצות מראש - האישיות, כל רשימה משותפת, וכל
  קטגוריה בתוכה - והוידג׳ט בוחר אחת לפי `id` שנשמר במופע. הוא אינו
  מסנן ואינו מקבץ, ולכן אפשר לשנות את החיתוך בעדכון חי.
- **המזהה של פריט בוידג׳ט קובע לאן הסימון חוזר.** אותו קופסה בדיוק כמו
  בכרטיס במסך: תזכורת אישית חיה בחנות המקומית ופריט משותף בענן, והם
  נראים זהים. לכן פריט במקור משותף נושא `sharedRef` ולא מזהה מופע,
  והשורה מעבירה `EXTRA_SHARED` בכוונת המילוי - הפעולה עצמה נקבעת פעם
  אחת לכל הוידג׳ט ואינה יכולה לשאת את ההבחנה. מזהה מהסוג הלא נכון אינו
  נכשל: הוא פשוט לא נמצא, והלחיצה נבלעת.
- **אותו פריט יושב בכמה מקומות בתמונת המצב** - במקור הרשימה, במקור
  הקטגוריה, ובמסמך של הוידג׳ט המשותף. `WidgetActionReceiver` קובע את
  הסימון פעם אחת וכותב אותו לכולם; אחרת הוא היה מתהפך חזרה בציור הבא של
  וידג׳ט אחר.

ועוד שתי מלכודות של RemoteViews, ושתיהן נכשלות בזמן ריצה ולא בקומפילציה:

- **מרשה לקרוא רק למתודות מסומנות.** `setBackgroundResource` מותר,
  `setPaintFlags` לא.
- **מרשה לנפח רק מחלקות מסומנות ב-`@RemoteView`.** `android.view.View`
  אינה מסומנת, ולכן תג `<View>` בפריסת וידג׳ט - אפילו כמפריד או כנקודה
  דקורטיבית - מפיל את הניפוח אצל המשגר, והמשתמש רואה "בעיה בטעינת
  הוידג׳ט" בלי שום רמז. משתמשים ב-`<ImageView>`, שמתנהג זהה ומסומן.
  זה מה שהשבית את וידג׳ט הלוח, ויש בדיקה שסורקת את הפריסות ותופסת את זה.

### עדכונים

שני מסלולים, והאפליקציה בוחרת ביניהם לבד:

- **עדכון חי** - חבילת ה-web מוחלפת בתוך האפליקציה, בלי APK. זה הרוב.
- **התקנת APK** - כשהשתנה משהו נייטיבי.

מה שקובע הוא טביעת הקוד הנייטיבי: הקומיט האחרון שנגע ב-`android/`,
ב-`package-lock.json` או ב-`capacitor.config.ts`. היא מוטמעת בחבילה
בזמן הבנייה, וטביעה זהה פירושה שהמעטפת לא השתנתה.

שני המסלולים מתבצעים בתוך האפליקציה: גם התקנת APK מורידה ומתקינה מכאן,
דרך `HebUpdaterPlugin`, ולא דרך הדפדפן. מה שאנדרואיד מחייב ואי אפשר
לעקוף הוא שני אישורים - הרשאת "התקנת אפליקציות לא מוכרות" פעם אחת,
וחלון ההתקנה בכל עדכון.

**המשמעות בעבודה היומיומית:** שינוי ב-`src/` לבדו מגיע למכשיר בלי
התקנה. הוספת פלאגין, הרשאה או אייקון מחייבת התקנת APK - ולכן כדאי
לקבץ שינויים כאלה.

**גוף השחרור הוא "מה השתנה", ותו לא.** `UpdateSheet` מציג את מספר
הגרסה בכותרת המשנה ואת גוף השחרור כתוכן, ולכן ה-workflow כותב לשם את
שורת הקומיט האחרונה בלבד - בלי מספר גרסה כפול ובלי טביעת קומיט, שאינה
אומרת דבר למי שמתקין. המשמעות: **שורת הקומיט היא הטקסט שהמשתמש קורא.**

`notifyBundleReady()` נקרא ראשון בעלייה. חבילה שלא מדווחת שהיא מוכנה
בתוך הזמן הקצוב מתגלגלת אחורה לבד; זו רשת הביטחון היחידה מפני חבילה
שבורה, ואסור להסיר אותה.

דברים שקל לשכוח:

- **הפריסה מקצה לקצה, והמידות מגיעות מהמערכת.** `env(safe-area-inset-*)`
  הוא אפס ב-WebView - הוא מתאר חריץ במסך ולא סרגלי מערכת. `MainActivity`
  מודדת ומזריקה `--inset-top/bottom/start/end`, ו-`.safe-t`/`.safe-b`
  לוקחים את הגדול מבין השניים. כותרת חדשה חייבת לעבור דרכם.
- **בחירת טקסט כבויה בגוף המסמך.** לחיצה ארוכה היא מחוות גרירה, לא
  "העתק". שדות קלט והערות הם החריג, דרך `input, textarea, .selectable`.
- **לכל התראה יש ערוץ.** `channelFor()` ב-`native.ts` גוזר אותו ממזהה
  התזכורת (`event-`, `place-`, והשאר שבת). בלי ערוץ המשתמש לא יכול
  להשתיק סוג אחד בלי להשתיק את כולם.
- **הפונט ארוז, לא נטען.** `npm run fonts` מוריד מגוגל ל-`public/fonts`,
  ורק תת-הקבוצות עברית ולטינית. אין יותר בקשת רשת בפתיחה.
- **ייצוא קובץ עובר דרך `shareTextFile()`.** `<a download>` על Blob לא
  עושה כלום ב-WebView, בלי שגיאה ובלי רמז.

- **מזהה החבילה הוא `com.ohav.hebcal`.** שינוי שלו מנתק את החתימה ואת
  רישום OAuth של גוגל, ומחייב התקנה מאפס.
- **מפתח החתימה לעולם לא במאגר.** הוא מגיע מסוד ב-Actions, וה-CI כותב
  אותו מחוץ לעץ העבודה. בלעדיו הבנייה עדיין עוברת, בחתימת דיבאג.
- **המשאבים הגרפיים נוצרים, לא נערכים ידנית.** `npm run icons:android`
  גוזר אותם מ-`scripts/logo.mjs`. אייקון ההתראה חייב להיות לבן על שקוף,
  כי אנדרואיד צובע אותו בעצמו.
- **הבנייה לאנדרואיד היא `PREVIEW_BUILD=1`** - בלי service worker. בתוך
  WebView הוא רק היה מחזיק גרסה ישנה במטמון.

## שבת ומועדים

**הניסוח נגזר מהמועד, ולא קבוע.** הכרטיס אמר "הדלקת נרות" ו"צאת השבת"
תמיד - כלומר על יום כיפור ועל סוכות הוא היה שקר קטן. `occasionLabels`
ב-`hebrew.ts` הוא המקור, והוא מבחין בין מה שנכנס בערב הראשון לבין מה
שיוצא בערב האחרון: **`isHoliday` ו-`endsHoliday` הם שני שדות נפרדים**,
כי ראש השנה תשפ״ז נכנס בערב שבת ויוצא במוצאי יום ראשון.

זמן הכניסה נשאר זמן הדלקת הנרות, כי זה גם זמן הכניסה הנהוג - גם ביום
טוב שנכנס במוצאי שבת hebcal נותן זמן הדלקה, ושם הוא זמן הכניסה. מה
שהשתנה הוא המילים בלבד.

התוויות נוסעות גם לוידג׳ט השבת (`entryLabel`/`exitLabel`), ולא נגזרות
שם: בפריסה היו שתי מחרוזות קבועות. מעטפת ישנה שאינה מקבלת אותן נשארת
עם מה שכתוב בפריסה - אותה נפילה לאחור של כל שדה חדש בוידג׳ט.

## אבטחה

ראו `SECURITY.md`. שתי נקודות לזכור:

- מפתחות Firebase של web אינם סודות ולא צריך להסתיר אותם. מה שכן סוד -
  `FIREBASE_TOKEN`, service accounts, `.env.local` - לעולם לא נכנס למאגר.
- **כלל אחד שמתיר - מתיר.** ב-Firestore כללים תואמים מתאחדים ב-OR, ולכן
  `match /{rest=**}` שמתיר מבטל בשקט כל כלל מחמיר יותר על אותו נתיב. תחת
  `users/` הייתה תבנית כזו, והיא הפכה את `validEvent` ואת כל חסמי הגודל
  לקוד מת - אירוע בלי כותרת, עם שדות זרים ובגודל בלי גבול נכתב בלי
  התנגדות. היא הוסרה, ולכל נתיב יש כלל משלו. נתיב חדש תחת `users/` חייב
  כלל משלו, ויש בדיקות ב-`rules-check` שתופסות את החזרה של התבנית.
- ה-CSP מרשה סקריפט מוטמע אחד לפי חתימת תוכן. עריכה של הסקריפט
  ב-`index.html` בלי עדכון החתימה תשבית אותו בשקט; יש בדיקה שתופסת את זה.

## בדיקות

הבדיקות נשענות על תאריכים ידועים מראש ולא על פלט הקוד:

```
ראש השנה תשפ״ז   1 בתשרי   → 12 בספטמבר 2026
יום כיפור        10 בתשרי  → 21 בספטמבר 2026
סוכות            15 בתשרי  → 26 בספטמבר 2026
פסח תשפ״ו        15 בניסן  → 2 באפריל 2026
תשפ״ז שנה מעוברת, תשפ״ו פשוטה
```

בדיקה שמשווה לפלט הקוד עצמה תעבור גם אחרי שהקוד יישבר. בדיקה שמשווה
לתאריך ידוע תתפוס את זה.
