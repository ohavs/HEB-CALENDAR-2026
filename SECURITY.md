# אבטחה

## מודל האיום

האפליקציה מחזיקה נתונים אישיים: אירועים, מיקומים שמורים, והעדפות. הכול
נשמר קודם כול במכשיר, ומסונכרן לענן רק אחרי התחברות מרצון.

שלושה גבולות מגינים על הנתונים:

1. **Firestore Security Rules** — כל נתיב מוגבל לבעליו. אין שום נתיב
   שנגיש לאנונימיים או חוצה משתמשים.
2. **Authorized domains** ב-Firebase Authentication — מגביל מאיזה מקור
   אפשר בכלל להתחבר.
3. **כותרות HTTP** ב-`firebase.json` — מצמצמות את מה שדף שנפרץ יכול
   לעשות.

## מפתחות Firebase אינם סודות

מפתחות אפליקציית web של Firebase הם מזהים ציבוריים מעצם טיבם: הם מוטמעים
בחבילת ה-JavaScript שנשלחת לכל דפדפן, ואי אפשר להסתיר אותם. ההגנה מתבצעת
בכללי האבטחה ובדומיינים המורשים, ולא בהסתרת המפתח.

מה שכן סוד, ולעולם לא נכנס למאגר:

- `FIREBASE_TOKEN` (הפלט של `firebase login:ci`) — נשמר כסוד ב-GitHub Actions
- קובצי service account
- `.env.local`

`.gitignore` חוסם `.env.*` ואת קובצי ה-service account. `.firebaserc` **כן**
נמצא במאגר, כי מזהה הפרויקט אינו סוד.

## כותרות HTTP

| כותרת | ערך | למה |
|---|---|---|
| `X-Content-Type-Options` | `nosniff` | מונע מהדפדפן לנחש סוג תוכן |
| `Referrer-Policy` | `strict-origin-when-cross-origin` | לא מדליף נתיבים לצד שלישי |
| `X-Frame-Options` | `SAMEORIGIN` | מונע clickjacking |
| `Permissions-Policy` | `geolocation=(self)`, השאר סגור | האפליקציה משתמשת במיקום; אין לה עסק עם מצלמה, מיקרופון או תשלומים |
| `Cross-Origin-Opener-Policy` | `same-origin-allow-popups` | בידוד, אבל בלי לשבור את חלון ההתחברות של גוגל |

### CSP

מדיניות התוכן מוגדרת כרגע כ-**Report-Only**. הסיבה: התחברות גוגל עוברת
דרך `signInWithPopup` עם נפילה ל-`signInWithRedirect`, ושני המסלולים
נוגעים בכמה מקורות של גוגל. מדיניות אוכפת שחוסמת אחד מהם שוברת את
ההתחברות בשקט, למשתמשים אמיתיים.

**להפעלה מלאה:** לפרוס, לפתוח את האפליקציה, להתחבר ולהתנתק, לבדוק את
הקונסול שאין דיווחי הפרה, ואז לשנות ב-`firebase.json` את
`Content-Security-Policy-Report-Only` ל-`Content-Security-Policy`.

`'unsafe-inline'` ב-`style-src` נדרש מפני שסגנונות מוטמעים משמשים
לאנימציות (framer-motion כותב `style` ישירות). אין `'unsafe-inline'`
ב-`script-src`, וזה מה שחשוב.

## כללי Firestore

```
/users/{uid}                  רק הבעלים
/users/{uid}/events/{id}      רק הבעלים, ועם אימות מבנה על כתיבה
/users/{uid}/meta/{doc}       רק הבעלים
/users/{uid}/{rest=**}        רק הבעלים
```

כל נתיב אחר נדחה כברירת מחדל.

אימות המבנה על אירוע (`validEvent`) מגביל את שמות השדות, את אורך הכותרת,
ההערות והמיקום, ואת מספר החריגים למסמך. הוא מגן על שני דברים: שמסמך פגום
מלקוח ישן לא ישבור את הלקוחות האחרים, ושמסמך יחיד לא יתפח בלי גבול.

## מה שלא נעשה, ולמה

- **אין הצפנה מקצה לקצה.** היא הייתה שוברת חיפוש בצד השרת ואת האפשרות
  לשתף לוחות בעתיד, ומודל האיום כאן (נתוני לוח שנה אישיים מול ספק ענן
  מהימן) אינו מצדיק אותה.
- **אין הגבלת קצב בצד הלקוח.** Firebase מטפל בזה ברמת הפרויקט.

## דיווח על תקלת אבטחה

לפתוח issue פרטי במאגר, או ליצור קשר ישירות. נא לא לפרסם פרטים פומבית
לפני שיש תיקון.
